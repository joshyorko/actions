# Important!!
# This file is auto-generated by running:
#
#   python _gen_workflows.py
#
# Do not edit it directly, your changes will be lost!
# Instead, edit `_gen_workflows.py` with the required
# changes and rerun that script (or manually change
# this file, but before the final version
# make sure that you update the `_gen_workflows.py`
# script to match the changes you made here
# and rerun the script).


name: Actions Runtime Recovery
permissions:
  contents: read
  actions: read
'on':
  workflow_dispatch:
    inputs:
      release_ref:
        description: Immutable actions-runtime release tag
        required: true
        type: string
      release_sha:
        description: Full 40-hex commit resolved by the release tag
        required: true
        type: string
      pypi_source_run_id:
        description: Failed canonical PyPI run whose successful component artifacts
          are reused
        required: false
        default: '31755673247'
        type: string
      pypi_source_workflow_id:
        description: Canonical Runtime PyPI workflow database ID
        required: false
        default: '333870965'
        type: string
defaults:
  run:
    working-directory: .
jobs:
  validate:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
    defaults:
      run:
        working-directory: .
    steps:
    - name: Checkout merged recovery code
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ github.workflow_sha }}
        path: recovery-code
        fetch-depth: 0
        persist-credentials: false
    - name: Admit only merged community recovery code
      working-directory: .
      shell: bash
      env:
        WORKFLOW_REF: ${{ github.workflow_ref }}
        WORKFLOW_SHA: ${{ github.workflow_sha }}
        REPOSITORY: ${{ github.repository }}
      run: 'set -Eeuo pipefail

        test "$REPOSITORY" = "joshyorko/actions"

        test "$WORKFLOW_REF" = "joshyorko/actions/.github/workflows/actions_runtime_recovery.yml@refs/heads/community"

        [[ "$WORKFLOW_SHA" =~ ^[0-9a-f]{40}$ ]]

        git -C recovery-code fetch --no-tags origin "refs/heads/community:refs/remotes/origin/community"

        test "$(git -C recovery-code rev-parse HEAD)" = "$WORKFLOW_SHA"

        test "$(git -C recovery-code rev-parse refs/remotes/origin/community)" = "$WORKFLOW_SHA"

        '
    - name: Checkout immutable release source
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ inputs.release_sha }}
        path: release-source
        fetch-depth: 0
        persist-credentials: false
    - name: Verify immutable tag, ancestry, and package version
      shell: bash
      working-directory: .
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
        RELEASE_SHA: ${{ inputs.release_sha }}
      run: "set -Eeuo pipefail\n[[ \"$RELEASE_REF\" =~ ^actions-runtime-[0-9]+\\.[0-9]+\\\
        .[0-9]+$ ]]\n[[ \"$RELEASE_SHA\" =~ ^[0-9a-f]{40}$ ]]\nif [[ \"$RELEASE_REF\"\
        \ == \"actions-runtime-1.0.1\" ]]; then\n  test \"$RELEASE_SHA\" = \"0accc8f8cd1590957dec63d94e091aa945653614\"\
        \nfi\ntest \"$(git -C release-source rev-parse HEAD)\" = \"$RELEASE_SHA\"\n\
        git -C release-source fetch --no-tags origin \"refs/tags/$RELEASE_REF:refs/tags/$RELEASE_REF\"\
        \ \"refs/heads/community:refs/remotes/origin/community\"\ntest \"$(git -C\
        \ release-source rev-parse \"refs/tags/$RELEASE_REF^{commit}\")\" = \"$RELEASE_SHA\"\
        \ngit -C release-source merge-base --is-ancestor \"$RELEASE_SHA\" refs/remotes/origin/community\n"
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081
      with:
        enable-cache: true
    - name: Install Python 3.12
      run: uv python install 3.12
    - name: Install devutils requirements
      run: uv run --no-project --python 3.12  python -m pip install --break-system-packages
        -r ../devutils/requirements.txt
      working-directory: release-source/action_server
    - name: Verify immutable Runtime source version
      shell: bash
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      working-directory: release-source/action_server
      run: 'set -Eeuo pipefail

        tag_version="${RELEASE_REF#actions-runtime-}"

        package_version=$(uv run --no-project --python 3.12 poetry version --short)

        test "$package_version" = "$tag_version"

        '
  pypi-recovery:
    needs:
    - validate
    if: ${{ needs.validate.result == 'success' && inputs.release_ref != 'actions-runtime-1.0.1'
      }}
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      actions: read
    defaults:
      run:
        working-directory: .
    steps:
    - name: Checkout merged recovery code
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ github.workflow_sha }}
        path: recovery-code
        fetch-depth: 0
        persist-credentials: false
    - name: Admit only merged community recovery code
      working-directory: .
      shell: bash
      env:
        WORKFLOW_REF: ${{ github.workflow_ref }}
        WORKFLOW_SHA: ${{ github.workflow_sha }}
        REPOSITORY: ${{ github.repository }}
      run: 'set -Eeuo pipefail

        test "$REPOSITORY" = "joshyorko/actions"

        test "$WORKFLOW_REF" = "joshyorko/actions/.github/workflows/actions_runtime_recovery.yml@refs/heads/community"

        [[ "$WORKFLOW_SHA" =~ ^[0-9a-f]{40}$ ]]

        git -C recovery-code fetch --no-tags origin "refs/heads/community:refs/remotes/origin/community"

        test "$(git -C recovery-code rev-parse HEAD)" = "$WORKFLOW_SHA"

        test "$(git -C recovery-code rev-parse refs/remotes/origin/community)" = "$WORKFLOW_SHA"

        '
    - name: Checkout immutable release source
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ inputs.release_sha }}
        path: release-source
        fetch-depth: 0
        persist-credentials: false
    - name: Verify immutable tag, ancestry, and package version
      shell: bash
      working-directory: .
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
        RELEASE_SHA: ${{ inputs.release_sha }}
      run: "set -Eeuo pipefail\n[[ \"$RELEASE_REF\" =~ ^actions-runtime-[0-9]+\\.[0-9]+\\\
        .[0-9]+$ ]]\n[[ \"$RELEASE_SHA\" =~ ^[0-9a-f]{40}$ ]]\nif [[ \"$RELEASE_REF\"\
        \ == \"actions-runtime-1.0.1\" ]]; then\n  test \"$RELEASE_SHA\" = \"0accc8f8cd1590957dec63d94e091aa945653614\"\
        \nfi\ntest \"$(git -C release-source rev-parse HEAD)\" = \"$RELEASE_SHA\"\n\
        git -C release-source fetch --no-tags origin \"refs/tags/$RELEASE_REF:refs/tags/$RELEASE_REF\"\
        \ \"refs/heads/community:refs/remotes/origin/community\"\ntest \"$(git -C\
        \ release-source rev-parse \"refs/tags/$RELEASE_REF^{commit}\")\" = \"$RELEASE_SHA\"\
        \ngit -C release-source merge-base --is-ancestor \"$RELEASE_SHA\" refs/remotes/origin/community\n"
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081
      with:
        enable-cache: true
    - name: Install Python 3.12
      run: uv python install 3.12
    - name: Install devutils requirements
      run: uv run --no-project --python 3.12  python -m pip install --break-system-packages
        -r ../devutils/requirements.txt
      working-directory: release-source/action_server
    - name: Verify immutable Runtime source version
      shell: bash
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      working-directory: release-source/action_server
      run: 'set -Eeuo pipefail

        tag_version="${RELEASE_REF#actions-runtime-}"

        package_version=$(uv run --no-project --python 3.12 poetry version --short)

        test "$package_version" = "$tag_version"

        '
    - name: Validate retained failed-run PyPI components
      env:
        PYPI_SOURCE_RUN_ID: ${{ inputs.pypi_source_run_id }}
        PYPI_SOURCE_WORKFLOW_ID: ${{ inputs.pypi_source_workflow_id }}
        RELEASE_REF: ${{ inputs.release_ref }}
        RELEASE_SHA: ${{ inputs.release_sha }}
        GH_TOKEN: ${{ github.token }}
      run: "set -Eeuo pipefail\ntest -n \"$PYPI_SOURCE_RUN_ID\"\ntest -n \"$PYPI_SOURCE_WORKFLOW_ID\"\
        \ngh_api_headers=(-H \"Accept: application/vnd.github+json\" -H \"X-GitHub-Api-Version:\
        \ 2022-11-28\")\nworkflow_json=$(gh api \"repos/$GITHUB_REPOSITORY/actions/workflows/actions_runtime_pypi_release.yml\"\
        \ \"${gh_api_headers[@]}\")\ntest \"$(jq -r '.id' <<<\"$workflow_json\")\"\
        \ = \"$PYPI_SOURCE_WORKFLOW_ID\"\ntest \"$(jq -r '.path' <<<\"$workflow_json\"\
        )\" = \".github/workflows/actions_runtime_pypi_release.yml\"\ntest \"$(jq\
        \ -r '.state' <<<\"$workflow_json\")\" = \"active\"\nrun_json=$(gh api \"\
        repos/$GITHUB_REPOSITORY/actions/runs/$PYPI_SOURCE_RUN_ID\" \"${gh_api_headers[@]}\"\
        )\njq -e --arg sha \"$RELEASE_SHA\" --arg ref \"$RELEASE_REF\" --arg workflow\
        \ \"$PYPI_SOURCE_WORKFLOW_ID\" --arg run \"$PYPI_SOURCE_RUN_ID\" --arg repo\
        \ \"$GITHUB_REPOSITORY\" '\n  .id == ($run | tonumber)\n  and .run_attempt\
        \ == 1\n  and .workflow_id == ($workflow | tonumber)\n  and .path == \".github/workflows/actions_runtime_pypi_release.yml\"\
        \n  and .head_sha == $sha\n  and .head_branch == $ref\n  and .event == \"\
        push\"\n  and .conclusion == \"failure\"\n  and .repository.full_name == $repo\n\
        \  and .head_repository.full_name == $repo\n' <<<\"$run_json\"\njobs_json=$(gh\
        \ api \"repos/$GITHUB_REPOSITORY/actions/runs/$PYPI_SOURCE_RUN_ID/jobs?per_page=100\"\
        \ \"${gh_api_headers[@]}\")\njq -e --argjson required '[\"build-sdist (ubuntu-py3.12)\"\
        , \"build-wheels (macos-15)\", \"build-wheels (ubuntu-22.04)\", \"build-wheels\
        \ (windows-2022)\"]' '\n  [.jobs[] | select(.name as $name | ($required |\
        \ index($name)) != null)] as $selected\n  | ($selected | length == 4)\n  and\
        \ (($selected | map(.name) | sort) == ($required | sort))\n  and ($selected\
        \ | all(.[]; .status == \"completed\" and .conclusion == \"success\"))\n \
        \ and ([.jobs[] | select(.name == \"publish (3.12)\")] | length == 1)\n  and\
        \ ([.jobs[] | select(.name == \"publish (3.12)\")][0].conclusion == \"failure\"\
        )\n  and ([.jobs[] | select(.name == \"publish (3.12)\")][0].steps[] | select(.name\
        \ == \"Verify merged tag provenance and version\") | .conclusion) == [\"failure\"\
        ]\n' <<<\"$jobs_json\"\nartifacts_json=$(gh api \"repos/$GITHUB_REPOSITORY/actions/runs/$PYPI_SOURCE_RUN_ID/artifacts?per_page=100\"\
        \ \"${gh_api_headers[@]}\")\nexpected_artifacts='[{\"id\":9202638277,\"name\"\
        :\"action-server-dist\",\"size_in_bytes\":848656,\"digest\":\"sha256:e68002161c7c05c7558339816733e56fc953fd8fe1bbf02f99f1f70c4a575072\"\
        },{\"id\":9202661215,\"name\":\"Linux-wheels\",\"size_in_bytes\":26180637,\"\
        digest\":\"sha256:e63ebadb20107adba8a6c76489105339337c1406db96ff328161dda9baf0c34e\"\
        },{\"id\":9202659672,\"name\":\"macOS-wheels\",\"size_in_bytes\":24523055,\"\
        digest\":\"sha256:5bba95082475ec10810edc3cf51a7a905ac135fd3fc58246be0f0244b53e281e\"\
        },{\"id\":9202679653,\"name\":\"Windows-wheels\",\"size_in_bytes\":21134688,\"\
        digest\":\"sha256:24daf1623d5b770b877b6e6d0b590b90b7b6d75f258b39cc1a20e30ca584f359\"\
        }]'\n# shellcheck disable=SC2016\njq_filter='(.artifacts | type == \"array\"\
        \ and length == 4) as $shape\n  | if ($shape | not) then false\n    else ([.artifacts[]\
        \ | if type != \"object\" then false\n      else (has(\"id\") and has(\"name\"\
        ) and has(\"size_in_bytes\") and has(\"digest\") and has(\"expired\") and\
        \ has(\"workflow_run\")\n        and (.id | type == \"number\" and floor ==\
        \ . and . > 0)\n        and (.name | type == \"string\" and length > 0)\n\
        \        and (.size_in_bytes | type == \"number\" and floor == . and . >=\
        \ 0)\n        and (.digest | type == \"string\" and test(\"^sha256:[0-9a-f]{64}$\"\
        ))\n        and (.expired == false)\n        and (if (.workflow_run | type)\
        \ != \"object\" then false\n          else (.workflow_run | has(\"id\"))\n\
        \            and (.workflow_run.id | type == \"number\" and floor == . and\
        \ . == $run)\n          end)\n      ) end] | all) as $valid\n      | if ($valid\
        \ | not) then false\n        else ([.artifacts[] | {id,name,size_in_bytes,digest,expired,workflow_run_id:.workflow_run.id}]\
        \ as $actual\n          | ($expected | all(.[]; . as $wanted | ([$actual[]\
        \ | select(.name == $wanted.name)] | length == 1 and .[0].id == $wanted.id\
        \ and .[0].name == $wanted.name and .[0].size_in_bytes == $wanted.size_in_bytes\
        \ and .[0].digest == $wanted.digest))))\n        end\n    end'\nif ! jq -e\
        \ --argjson run \"$PYPI_SOURCE_RUN_ID\" --argjson expected \"$expected_artifacts\"\
        \ \"$jq_filter\" <<<\"$artifacts_json\"; then\n  echo \"artifact inventory\
        \ validation failed; raw metadata withheld\" >&2\n  exit 1\nfi\n"
    - name: Download retained action-server-dist
      env:
        ARTIFACT_ID: '9202638277'
        DESTINATION: pypi-components/sdist
        GH_TOKEN: ${{ github.token }}
      run: "set -Eeuo pipefail\nmkdir -p \"$DESTINATION\"\ngh api \"repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip\"\
        \ > /tmp/runtime-artifact.zip\ntest \"$(sha256sum /tmp/runtime-artifact.zip\
        \ | cut -d' ' -f1)\" = \"e68002161c7c05c7558339816733e56fc953fd8fe1bbf02f99f1f70c4a575072\"\
        \nDESTINATION=\"$DESTINATION\" python - <<'PY'\nimport os\nimport posixpath\n\
        import stat\nimport zipfile\nfrom pathlib import PurePosixPath\n\ndestination\
        \ = os.environ[\"DESTINATION\"]\nroot = os.path.realpath(destination)\nseen_members\
        \ = set()\nwith zipfile.ZipFile(\"/tmp/runtime-artifact.zip\") as source:\n\
        \    for member in source.infolist():\n        canonical_name = posixpath.normpath(member.filename)\n\
        \        path = PurePosixPath(canonical_name)\n        if path.is_absolute()\
        \ or \"..\" in path.parts or \"\\x00\" in member.filename:\n            raise\
        \ SystemExit(f\"unsafe archive member: {member.filename!r}\")\n        mode\
        \ = (member.external_attr >> 16) & 0o177777\n        if mode and stat.S_IFMT(mode)\
        \ not in (0, stat.S_IFDIR, stat.S_IFREG):\n            raise SystemExit(f\"\
        unsafe archive link or special member: {member.filename!r}\")\n        if\
        \ canonical_name in seen_members:\n            raise SystemExit(f\"duplicate\
        \ archive member: {member.filename!r}\")\n        seen_members.add(canonical_name)\n\
        \        target = os.path.realpath(os.path.join(destination, *path.parts))\n\
        \        if os.path.commonpath((root, target)) != root:\n            raise\
        \ SystemExit(f\"archive member escapes destination: {member.filename!r}\"\
        )\n        if member.is_dir():\n            os.makedirs(target, exist_ok=True)\n\
        \            continue\n        os.makedirs(os.path.dirname(target), exist_ok=True)\n\
        \        with source.open(member) as input_file, open(target, \"xb\") as output_file:\n\
        \            output_file.write(input_file.read())\nPY\n"
    - name: Download retained Linux-wheels
      env:
        ARTIFACT_ID: '9202661215'
        DESTINATION: pypi-components/linux
        GH_TOKEN: ${{ github.token }}
      run: "set -Eeuo pipefail\nmkdir -p \"$DESTINATION\"\ngh api \"repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip\"\
        \ > /tmp/runtime-artifact.zip\ntest \"$(sha256sum /tmp/runtime-artifact.zip\
        \ | cut -d' ' -f1)\" = \"e63ebadb20107adba8a6c76489105339337c1406db96ff328161dda9baf0c34e\"\
        \nDESTINATION=\"$DESTINATION\" python - <<'PY'\nimport os\nimport posixpath\n\
        import stat\nimport zipfile\nfrom pathlib import PurePosixPath\n\ndestination\
        \ = os.environ[\"DESTINATION\"]\nroot = os.path.realpath(destination)\nseen_members\
        \ = set()\nwith zipfile.ZipFile(\"/tmp/runtime-artifact.zip\") as source:\n\
        \    for member in source.infolist():\n        canonical_name = posixpath.normpath(member.filename)\n\
        \        path = PurePosixPath(canonical_name)\n        if path.is_absolute()\
        \ or \"..\" in path.parts or \"\\x00\" in member.filename:\n            raise\
        \ SystemExit(f\"unsafe archive member: {member.filename!r}\")\n        mode\
        \ = (member.external_attr >> 16) & 0o177777\n        if mode and stat.S_IFMT(mode)\
        \ not in (0, stat.S_IFDIR, stat.S_IFREG):\n            raise SystemExit(f\"\
        unsafe archive link or special member: {member.filename!r}\")\n        if\
        \ canonical_name in seen_members:\n            raise SystemExit(f\"duplicate\
        \ archive member: {member.filename!r}\")\n        seen_members.add(canonical_name)\n\
        \        target = os.path.realpath(os.path.join(destination, *path.parts))\n\
        \        if os.path.commonpath((root, target)) != root:\n            raise\
        \ SystemExit(f\"archive member escapes destination: {member.filename!r}\"\
        )\n        if member.is_dir():\n            os.makedirs(target, exist_ok=True)\n\
        \            continue\n        os.makedirs(os.path.dirname(target), exist_ok=True)\n\
        \        with source.open(member) as input_file, open(target, \"xb\") as output_file:\n\
        \            output_file.write(input_file.read())\nPY\n"
    - name: Download retained macOS-wheels
      env:
        ARTIFACT_ID: '9202659672'
        DESTINATION: pypi-components/macos
        GH_TOKEN: ${{ github.token }}
      run: "set -Eeuo pipefail\nmkdir -p \"$DESTINATION\"\ngh api \"repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip\"\
        \ > /tmp/runtime-artifact.zip\ntest \"$(sha256sum /tmp/runtime-artifact.zip\
        \ | cut -d' ' -f1)\" = \"5bba95082475ec10810edc3cf51a7a905ac135fd3fc58246be0f0244b53e281e\"\
        \nDESTINATION=\"$DESTINATION\" python - <<'PY'\nimport os\nimport posixpath\n\
        import stat\nimport zipfile\nfrom pathlib import PurePosixPath\n\ndestination\
        \ = os.environ[\"DESTINATION\"]\nroot = os.path.realpath(destination)\nseen_members\
        \ = set()\nwith zipfile.ZipFile(\"/tmp/runtime-artifact.zip\") as source:\n\
        \    for member in source.infolist():\n        canonical_name = posixpath.normpath(member.filename)\n\
        \        path = PurePosixPath(canonical_name)\n        if path.is_absolute()\
        \ or \"..\" in path.parts or \"\\x00\" in member.filename:\n            raise\
        \ SystemExit(f\"unsafe archive member: {member.filename!r}\")\n        mode\
        \ = (member.external_attr >> 16) & 0o177777\n        if mode and stat.S_IFMT(mode)\
        \ not in (0, stat.S_IFDIR, stat.S_IFREG):\n            raise SystemExit(f\"\
        unsafe archive link or special member: {member.filename!r}\")\n        if\
        \ canonical_name in seen_members:\n            raise SystemExit(f\"duplicate\
        \ archive member: {member.filename!r}\")\n        seen_members.add(canonical_name)\n\
        \        target = os.path.realpath(os.path.join(destination, *path.parts))\n\
        \        if os.path.commonpath((root, target)) != root:\n            raise\
        \ SystemExit(f\"archive member escapes destination: {member.filename!r}\"\
        )\n        if member.is_dir():\n            os.makedirs(target, exist_ok=True)\n\
        \            continue\n        os.makedirs(os.path.dirname(target), exist_ok=True)\n\
        \        with source.open(member) as input_file, open(target, \"xb\") as output_file:\n\
        \            output_file.write(input_file.read())\nPY\n"
    - name: Download retained Windows-wheels
      env:
        ARTIFACT_ID: '9202679653'
        DESTINATION: pypi-components/windows
        GH_TOKEN: ${{ github.token }}
      run: "set -Eeuo pipefail\nmkdir -p \"$DESTINATION\"\ngh api \"repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip\"\
        \ > /tmp/runtime-artifact.zip\ntest \"$(sha256sum /tmp/runtime-artifact.zip\
        \ | cut -d' ' -f1)\" = \"24daf1623d5b770b877b6e6d0b590b90b7b6d75f258b39cc1a20e30ca584f359\"\
        \nDESTINATION=\"$DESTINATION\" python - <<'PY'\nimport os\nimport posixpath\n\
        import stat\nimport zipfile\nfrom pathlib import PurePosixPath\n\ndestination\
        \ = os.environ[\"DESTINATION\"]\nroot = os.path.realpath(destination)\nseen_members\
        \ = set()\nwith zipfile.ZipFile(\"/tmp/runtime-artifact.zip\") as source:\n\
        \    for member in source.infolist():\n        canonical_name = posixpath.normpath(member.filename)\n\
        \        path = PurePosixPath(canonical_name)\n        if path.is_absolute()\
        \ or \"..\" in path.parts or \"\\x00\" in member.filename:\n            raise\
        \ SystemExit(f\"unsafe archive member: {member.filename!r}\")\n        mode\
        \ = (member.external_attr >> 16) & 0o177777\n        if mode and stat.S_IFMT(mode)\
        \ not in (0, stat.S_IFDIR, stat.S_IFREG):\n            raise SystemExit(f\"\
        unsafe archive link or special member: {member.filename!r}\")\n        if\
        \ canonical_name in seen_members:\n            raise SystemExit(f\"duplicate\
        \ archive member: {member.filename!r}\")\n        seen_members.add(canonical_name)\n\
        \        target = os.path.realpath(os.path.join(destination, *path.parts))\n\
        \        if os.path.commonpath((root, target)) != root:\n            raise\
        \ SystemExit(f\"archive member escapes destination: {member.filename!r}\"\
        )\n        if member.is_dir():\n            os.makedirs(target, exist_ok=True)\n\
        \            continue\n        os.makedirs(os.path.dirname(target), exist_ok=True)\n\
        \        with source.open(member) as input_file, open(target, \"xb\") as output_file:\n\
        \            output_file.write(input_file.read())\nPY\n"
    - name: Combine and verify exact Runtime artifacts
      run: uv run --no-project --python 3.12 python recovery-code/action_server/scripts/publish_verified_runtime.py
        --download-root pypi-components --dry-run
    - name: Install Twine 6.2.0
      run: uv run --no-project --python 3.12 python -m pip install --break-system-packages
        twine==6.2.0
    - name: Verify Runtime artifacts
      run: uv run --no-project --python 3.12 twine check --strict actions-runtime-dist/*.whl
        actions-runtime-dist/*.tar.gz
    - name: Clean-install Runtime and run checks
      run: 'set -Eeuo pipefail

        rm -rf /tmp/actions-runtime-recovery-venv

        uv venv --python 3.12 /tmp/actions-runtime-recovery-venv

        linux_wheel=$(find actions-runtime-dist -type f -name ''*-cp312-cp312-manylinux*.whl''
        -print)

        test "$(printf ''%s\n'' "$linux_wheel" | sed ''/^$/d'' | wc -l)" -eq 1

        uv pip install --python /tmp/actions-runtime-recovery-venv/bin/python "$linux_wheel"

        /tmp/actions-runtime-recovery-venv/bin/python -m pip check

        /tmp/actions-runtime-recovery-venv/bin/python -m actions.server version

        '
    - name: 'Upload artifact: actions-runtime-dist/*'
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: actions-runtime-dist
        path: actions-runtime-dist/*
  binary-build:
    needs:
    - validate
    if: ${{ needs.validate.result == 'success' }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    defaults:
      run:
        working-directory: release-source/action_server
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        include:
        - os: ubuntu-22.04
          name: linux
          python: '3.12'
        - os: macos-15
          name: macos
          python: '3.12'
        - os: windows-2022
          name: windows
          python: '3.12'
    steps:
    - name: Checkout merged recovery code
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ github.workflow_sha }}
        path: recovery-code
        fetch-depth: 0
        persist-credentials: false
    - name: Admit only merged community recovery code
      working-directory: .
      shell: bash
      env:
        WORKFLOW_REF: ${{ github.workflow_ref }}
        WORKFLOW_SHA: ${{ github.workflow_sha }}
        REPOSITORY: ${{ github.repository }}
      run: 'set -Eeuo pipefail

        test "$REPOSITORY" = "joshyorko/actions"

        test "$WORKFLOW_REF" = "joshyorko/actions/.github/workflows/actions_runtime_recovery.yml@refs/heads/community"

        [[ "$WORKFLOW_SHA" =~ ^[0-9a-f]{40}$ ]]

        git -C recovery-code fetch --no-tags origin "refs/heads/community:refs/remotes/origin/community"

        test "$(git -C recovery-code rev-parse HEAD)" = "$WORKFLOW_SHA"

        test "$(git -C recovery-code rev-parse refs/remotes/origin/community)" = "$WORKFLOW_SHA"

        '
    - name: Checkout immutable release source
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ inputs.release_sha }}
        path: release-source
        fetch-depth: 0
        persist-credentials: false
    - name: Verify immutable tag, ancestry, and package version
      shell: bash
      working-directory: .
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
        RELEASE_SHA: ${{ inputs.release_sha }}
      run: "set -Eeuo pipefail\n[[ \"$RELEASE_REF\" =~ ^actions-runtime-[0-9]+\\.[0-9]+\\\
        .[0-9]+$ ]]\n[[ \"$RELEASE_SHA\" =~ ^[0-9a-f]{40}$ ]]\nif [[ \"$RELEASE_REF\"\
        \ == \"actions-runtime-1.0.1\" ]]; then\n  test \"$RELEASE_SHA\" = \"0accc8f8cd1590957dec63d94e091aa945653614\"\
        \nfi\ntest \"$(git -C release-source rev-parse HEAD)\" = \"$RELEASE_SHA\"\n\
        git -C release-source fetch --no-tags origin \"refs/tags/$RELEASE_REF:refs/tags/$RELEASE_REF\"\
        \ \"refs/heads/community:refs/remotes/origin/community\"\ntest \"$(git -C\
        \ release-source rev-parse \"refs/tags/$RELEASE_REF^{commit}\")\" = \"$RELEASE_SHA\"\
        \ngit -C release-source merge-base --is-ancestor \"$RELEASE_SHA\" refs/remotes/origin/community\n"
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081
      with:
        enable-cache: true
    - name: Install Python 3.12
      run: uv python install 3.12
    - name: Install devutils requirements
      run: uv run --no-project --python 3.12  python -m pip install --break-system-packages
        -r ../devutils/requirements.txt
      working-directory: release-source/action_server
    - name: Verify immutable Runtime source version
      shell: bash
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      working-directory: release-source/action_server
      run: 'set -Eeuo pipefail

        tag_version="${RELEASE_REF#actions-runtime-}"

        package_version=$(uv run --no-project --python 3.12 poetry version --short)

        test "$package_version" = "$tag_version"

        '
    - name: Install immutable Runtime source
      run: uv run --no-project --python 3.12 inv install
      working-directory: release-source/action_server
    - name: Setup node
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
      with:
        node-version: 20.x
    - name: Setup go
      uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff
      with:
        go-version: '1.23'
        cache-dependency-path: release-source/action_server/go-wrapper/go.sum
    - name: Build frontend from immutable source
      run: uv run --no-project --python ${{ matrix.python }} inv build-frontend
      env:
        CI: true
      working-directory: release-source/action_server
    - name: Build OAuth2 config from immutable source
      run: uv run --no-project --python ${{ matrix.python }} inv build-oauth2-config
      env:
        GH_TOKEN: ${{ secrets.GH_PAT_GHA_TO_ANOTHER_REPO }}
      working-directory: release-source/action_server
    - name: Verify immutable Runtime source version
      shell: bash
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      run: 'set -Eeuo pipefail

        tag_version="${RELEASE_REF#actions-runtime-}"

        package_version=$(uv run --no-project --python 3.12 poetry version --short)

        test "$package_version" = "$tag_version"

        '
      working-directory: release-source/action_server
    - name: Check signing availability
      id: signing
      shell: bash
      env:
        RC_ACTION_SERVER_FORCE_DOWNLOAD_RCC: 'true'
        RC_ACTION_SERVER_DO_SELFTEST: 'true'
        GITHUB_EVENT_NAME: ${{ github.event_name }}
        GITHUB_REF_NAME: ${{ inputs.release_ref }}
        GITHUB_PR_NUMBER: ${{ github.event.pull_request.number }}
        MACOS_SIGNING_CERT: ${{ secrets.MACOS_SIGNING_CERT_ACTIONS_RUNTIME }}
        MACOS_SIGNING_CERT_PASSWORD: ${{ secrets.MACOS_SIGNING_CERT_PASSWORD_ACTIONS_RUNTIME
          }}
        MACOS_SIGNING_CERT_NAME: ${{ secrets.MACOS_SIGNING_CERT_NAME_ACTIONS_RUNTIME
          }}
        APPLEID: ${{ secrets.MACOS_APP_ID_FOR_NOTARIZATION_ACTIONS_RUNTIME }}
        APPLETEAMID: ${{ secrets.MACOS_TEAM_ID_FOR_NOTARIZATION_ACTIONS_RUNTIME }}
        APPLEIDPASS: ${{ secrets.MACOS_APP_ID_PASSWORD_ACTIONS_RUNTIME }}
        VAULT_URL: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_URL_ACTIONS_RUNTIME }}
        CLIENT_ID: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_CLIENT_ID_ACTIONS_RUNTIME
          }}
        TENANT_ID: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_TENANT_ID_ACTIONS_RUNTIME
          }}
        CLIENT_SECRET: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_CLIENT_SECRET_ACTIONS_RUNTIME
          }}
        CERTIFICATE_NAME: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_CERTIFICATE_NAME_ACTIONS_RUNTIME
          }}
        SIGNING_EVENT: ${{ github.event_name }}
        SIGNING_OS: ${{ matrix.os }}
      run: "set -Eeuo pipefail\nsigned=false\ncase \"$SIGNING_OS\" in\n  macos-15)\
        \ keys=\"MACOS_SIGNING_CERT MACOS_SIGNING_CERT_PASSWORD MACOS_SIGNING_CERT_NAME\
        \ APPLEID APPLETEAMID APPLEIDPASS\" ;;\n  windows-2022) keys=\"VAULT_URL CLIENT_ID\
        \ TENANT_ID CLIENT_SECRET CERTIFICATE_NAME\" ;;\n  ubuntu-22.04) keys=\"\"\
        \ ;;\n  *) exit 1 ;;\nesac\npresent=0\nmissing=0\nfor key in $keys; do\n \
        \ if [[ -n \"${!key:-}\" ]]; then present=$((present + 1)); else missing=$((missing\
        \ + 1)); fi\ndone\nif (( present > 0 && missing > 0 )); then\n  echo \"partial\
        \ signing credentials configured\" >&2\n  exit 1\nfi\nif (( present > 0 ));\
        \ then signed=true; else echo \"all signing credentials absent; using unsigned\
        \ binary\"; fi\necho \"signed=$signed\" >> \"$GITHUB_OUTPUT\"\n"
    - name: Build binary (signed)
      if: ${{ steps.signing.outputs.signed == 'true' }}
      env:
        RC_ACTION_SERVER_FORCE_DOWNLOAD_RCC: 'true'
        RC_ACTION_SERVER_DO_SELFTEST: 'true'
        GITHUB_EVENT_NAME: ${{ github.event_name }}
        GITHUB_REF_NAME: ${{ inputs.release_ref }}
        GITHUB_PR_NUMBER: ${{ github.event.pull_request.number }}
        MACOS_SIGNING_CERT: ${{ secrets.MACOS_SIGNING_CERT_ACTIONS_RUNTIME }}
        MACOS_SIGNING_CERT_PASSWORD: ${{ secrets.MACOS_SIGNING_CERT_PASSWORD_ACTIONS_RUNTIME
          }}
        MACOS_SIGNING_CERT_NAME: ${{ secrets.MACOS_SIGNING_CERT_NAME_ACTIONS_RUNTIME
          }}
        APPLEID: ${{ secrets.MACOS_APP_ID_FOR_NOTARIZATION_ACTIONS_RUNTIME }}
        APPLETEAMID: ${{ secrets.MACOS_TEAM_ID_FOR_NOTARIZATION_ACTIONS_RUNTIME }}
        APPLEIDPASS: ${{ secrets.MACOS_APP_ID_PASSWORD_ACTIONS_RUNTIME }}
        VAULT_URL: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_URL_ACTIONS_RUNTIME }}
        CLIENT_ID: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_CLIENT_ID_ACTIONS_RUNTIME
          }}
        TENANT_ID: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_TENANT_ID_ACTIONS_RUNTIME
          }}
        CLIENT_SECRET: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_CLIENT_SECRET_ACTIONS_RUNTIME
          }}
        CERTIFICATE_NAME: ${{ secrets.WIN_SIGN_AZURE_KEY_VAULT_CERTIFICATE_NAME_ACTIONS_RUNTIME
          }}
      run: uv run --no-project --python ${{ matrix.python }} poetry run inv build-executable
        --sign --go-wrapper
    - name: Build binary (unsigned)
      if: ${{ steps.signing.outputs.signed != 'true' }}
      env:
        RC_ACTION_SERVER_FORCE_DOWNLOAD_RCC: 'true'
        RC_ACTION_SERVER_DO_SELFTEST: 'true'
        GITHUB_EVENT_NAME: ${{ github.event_name }}
        GITHUB_REF_NAME: ${{ inputs.release_ref }}
        GITHUB_PR_NUMBER: ${{ github.event.pull_request.number }}
      run: uv run --no-project --python ${{ matrix.python }} poetry run inv build-executable
        --go-wrapper
    - name: Record native signing status
      env:
        SIGNED: ${{ steps.signing.outputs.signed }}
      run: printf '%s\n' "$SIGNED" > signing-status.txt
    - name: 'Upload artifact: release-source/action_server/signing-status.txt'
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: native-signing-${{ matrix.name }}
        path: release-source/action_server/signing-status.txt
    - name: 'Upload artifact: release-source/action_server/dist/final/*'
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: actions-runtime-binary-${{ matrix.name }}
        path: release-source/action_server/dist/final/*
  binary-release:
    needs:
    - validate
    - binary-build
    if: ${{ needs.binary-build.result == 'success' }}
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      id-token: write
    defaults:
      run:
        working-directory: .
    steps:
    - name: Checkout merged recovery code
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ github.workflow_sha }}
        path: recovery-code
        fetch-depth: 0
        persist-credentials: false
    - name: Admit only merged community recovery code
      working-directory: .
      shell: bash
      env:
        WORKFLOW_REF: ${{ github.workflow_ref }}
        WORKFLOW_SHA: ${{ github.workflow_sha }}
        REPOSITORY: ${{ github.repository }}
      run: 'set -Eeuo pipefail

        test "$REPOSITORY" = "joshyorko/actions"

        test "$WORKFLOW_REF" = "joshyorko/actions/.github/workflows/actions_runtime_recovery.yml@refs/heads/community"

        [[ "$WORKFLOW_SHA" =~ ^[0-9a-f]{40}$ ]]

        git -C recovery-code fetch --no-tags origin "refs/heads/community:refs/remotes/origin/community"

        test "$(git -C recovery-code rev-parse HEAD)" = "$WORKFLOW_SHA"

        test "$(git -C recovery-code rev-parse refs/remotes/origin/community)" = "$WORKFLOW_SHA"

        '
    - name: Checkout immutable release source
      uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      with:
        ref: ${{ inputs.release_sha }}
        path: release-source
        fetch-depth: 0
        persist-credentials: false
    - name: Verify immutable tag, ancestry, and package version
      shell: bash
      working-directory: .
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
        RELEASE_SHA: ${{ inputs.release_sha }}
      run: "set -Eeuo pipefail\n[[ \"$RELEASE_REF\" =~ ^actions-runtime-[0-9]+\\.[0-9]+\\\
        .[0-9]+$ ]]\n[[ \"$RELEASE_SHA\" =~ ^[0-9a-f]{40}$ ]]\nif [[ \"$RELEASE_REF\"\
        \ == \"actions-runtime-1.0.1\" ]]; then\n  test \"$RELEASE_SHA\" = \"0accc8f8cd1590957dec63d94e091aa945653614\"\
        \nfi\ntest \"$(git -C release-source rev-parse HEAD)\" = \"$RELEASE_SHA\"\n\
        git -C release-source fetch --no-tags origin \"refs/tags/$RELEASE_REF:refs/tags/$RELEASE_REF\"\
        \ \"refs/heads/community:refs/remotes/origin/community\"\ntest \"$(git -C\
        \ release-source rev-parse \"refs/tags/$RELEASE_REF^{commit}\")\" = \"$RELEASE_SHA\"\
        \ngit -C release-source merge-base --is-ancestor \"$RELEASE_SHA\" refs/remotes/origin/community\n"
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081
      with:
        enable-cache: true
    - name: Install Python 3.12
      run: uv python install 3.12
    - name: Install devutils requirements
      run: uv run --no-project --python 3.12  python -m pip install --break-system-packages
        -r ../devutils/requirements.txt
      working-directory: release-source/action_server
    - name: Verify immutable Runtime source version
      shell: bash
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      working-directory: release-source/action_server
      run: 'set -Eeuo pipefail

        tag_version="${RELEASE_REF#actions-runtime-}"

        package_version=$(uv run --no-project --python 3.12 poetry version --short)

        test "$package_version" = "$tag_version"

        '
    - name: Download immutable linux binary
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: actions-runtime-binary-linux
        path: binaries/linux
    - name: Download immutable macos binary
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: actions-runtime-binary-macos
        path: binaries/macos
    - name: Download immutable windows binary
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: actions-runtime-binary-windows
        path: binaries/windows
    - name: Download linux signing receipt
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: native-signing-linux
        path: native-signing/linux
    - name: Download macos signing receipt
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: native-signing-macos
        path: native-signing/macos
    - name: Download windows signing receipt
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: native-signing-windows
        path: native-signing/windows
    - name: Validate exact binary set
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      run: 'set -Eeuo pipefail

        rm -rf release-assets

        mkdir -p release-assets

        test "$(find binaries/linux -type f | wc -l)" -eq 1

        test "$(find binaries/macos -type f | wc -l)" -eq 1

        test "$(find binaries/windows -type f | wc -l)" -eq 1

        test -s binaries/linux/action-server

        test -s binaries/macos/action-server

        test -s binaries/windows/action-server.exe

        cp binaries/linux/action-server "release-assets/${RELEASE_REF}-linux64"

        cp binaries/macos/action-server "release-assets/${RELEASE_REF}-macos-arm64"

        cp binaries/windows/action-server.exe "release-assets/${RELEASE_REF}-windows64.exe"

        find release-assets -maxdepth 1 -type f -printf ''%f\n'' | sort > /tmp/runtime-binary-assets

        printf ''%s\n'' "${RELEASE_REF}-linux64" "${RELEASE_REF}-macos-arm64" "${RELEASE_REF}-windows64.exe"
        | sort > /tmp/runtime-binary-expected

        diff -u /tmp/runtime-binary-expected /tmp/runtime-binary-assets

        test "$(wc -l < /tmp/runtime-binary-assets)" -eq 3

        test -z "$(uniq -d /tmp/runtime-binary-assets)"

        '
    - name: Create or update the complete GitHub release
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
        RELEASE_SHA: ${{ inputs.release_sha }}
        GH_TOKEN: ${{ github.token }}
      run: "set -Eeuo pipefail\nmanifest=$(mktemp)\nsha256sum release-assets/* | sed\
        \ 's#release-assets/##' | sort > \"$manifest\"\nexpected=$(jq -Rn '[inputs\
        \ | split(\"  \") | {name:.[1],digest:(\"sha256:\" + .[0])}]' < \"$manifest\"\
        )\nnotes=$(mktemp)\nprintf 'Runtime %s recovered from immutable source %s.\\\
        n\\n' \"$RELEASE_REF\" \"$RELEASE_SHA\" > \"$notes\"\nfor platform in linux\
        \ macos windows; do\n  signed=$(cat \"native-signing/$platform/signing-status.txt\"\
        )\n  case \"$signed\" in\n    true) printf '%s: signed.\\n' \"$platform\"\
        \ >> \"$notes\" ;;\n    false) printf '%s: UNSIGNED; no publisher code signature\
        \ or notarization.\\n' \"$platform\" >> \"$notes\" ;;\n    *) echo 'Invalid\
        \ signing receipt' >&2; exit 1 ;;\n  esac\ndone\nprintf '\\nSHA-256:\\n' >>\
        \ \"$notes\"\ncat \"$manifest\" >> \"$notes\"\nrelease_json=\"\"\nif release_json=$(gh\
        \ api \"repos/$GITHUB_REPOSITORY/releases/tags/$RELEASE_REF\"); then\n  test\
        \ \"$(jq -r '.tag_name' <<<\"$release_json\")\" = \"$RELEASE_REF\"\n  test\
        \ \"$(jq -r '.target_commitish' <<<\"$release_json\")\" = \"$RELEASE_SHA\"\
        \n  test \"$(jq -r '.draft' <<<\"$release_json\")\" = \"true\"\n  jq -e --argjson\
        \ expected \"$expected\" '([.assets[].name] - [$expected[].name] | length\
        \ == 0) and ([.assets[].name] | unique | length == length)' <<<\"$release_json\"\
        \n  while read -r digest name; do\n    existing_digest=$(jq -r --arg name\
        \ \"$name\" '[.assets[] | select(.name == $name)][0].digest' <<<\"$release_json\"\
        )\n    if [ \"$existing_digest\" = \"null\" ]; then\n      gh release upload\
        \ \"$RELEASE_REF\" \"release-assets/$name\"\n    else\n      test \"$existing_digest\"\
        \ = \"sha256:$digest\"\n    fi\n  done < \"$manifest\"\nelse\n  gh release\
        \ create \"$RELEASE_REF\" --draft --verify-tag --target \"$RELEASE_SHA\" --title\
        \ \"$RELEASE_REF\" --notes-file \"$notes\"\n  gh release upload \"$RELEASE_REF\"\
        \ release-assets/*\nfi\nrelease_json=$(gh api \"repos/$GITHUB_REPOSITORY/releases/tags/$RELEASE_REF\"\
        )\ntest \"$(jq -r '.tag_name' <<<\"$release_json\")\" = \"$RELEASE_REF\"\n\
        test \"$(jq -r '.target_commitish' <<<\"$release_json\")\" = \"$RELEASE_SHA\"\
        \ntest \"$(jq -r '.draft' <<<\"$release_json\")\" = \"true\"\nexpected_names=$(awk\
        \ '{print $2}' \"$manifest\" | sort)\nactual_names=$(jq -r '.assets[].name'\
        \ <<<\"$release_json\" | sort)\ntest \"$actual_names\" = \"$expected_names\"\
        \njq -e --argjson expected \"$expected\" '([.assets[] | {name,digest}] | sort_by(.name))\
        \ == ($expected | sort_by(.name))' <<<\"$release_json\"\ngh release edit \"\
        $RELEASE_REF\" --draft=false --notes-file \"$notes\"\n"
    - name: Stage recovered binaries for existing handoffs
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
      run: mkdir -p build && cp -R binaries/linux build/linux64 && cp -R binaries/macos
        build/macos-arm64 && cp -R binaries/windows build/windows64 && printf '%s\n'
        "${RELEASE_REF#actions-runtime-}" > build/version.txt
    - name: Put files in s3-drop
      run: "\nls -l\npwd\nls -l build\nmkdir s3-drop\nmv build/version.txt s3-drop/\n\
        mv build/macos-arm64 s3-drop/\nmv build/linux64 s3-drop/\nmv build/windows64\
        \ s3-drop/\nls -l s3-drop/\nver=$(cat s3-drop/version.txt)\necho \"actionServerVersion=${ver}\"\
        \ >> \"$GITHUB_ENV\"\nif [[ \"$GITHUB_REF_NAME\" == actions-runtime-* ]];\
        \ then\n  test \"$ver\" = \"${GITHUB_REF_NAME#actions-runtime-}\"\nfi\ntest\
        \ \"$(find s3-drop -type f | wc -l)\" -eq 4\ntest -f s3-drop/linux64/action-server\n\
        test -f s3-drop/macos-arm64/action-server\ntest -f s3-drop/windows64/action-server.exe\n"
    - name: 'Upload artifact: action_server/s3-drop'
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: action-server-artifacts-for-s3-${{ env.actionServerVersion }}
        path: s3-drop
    - name: Configure AWS credentials Dropbox bucket
      uses: aws-actions/configure-aws-credentials@b47578312673ae6fa5b5096b330d9fbac3d116df
      with:
        aws-region: eu-west-1
        role-to-assume: arn:aws:iam::710450854638:role/github-action-robocorp-action-server
    - name: AWS S3 copies
      run: "\nif [ \"false\" = \"false\" ]; then\n  echo \"Normal release, aws sync\
        \ to drop-box, full pipeline\"\n  aws s3 sync s3-drop s3://robocorp-action-server-build-drop-box\n\
        else\n  echo \"BETA RELEASE, only copy the executable for testing\"\n  S3_BASE_URL=\"\
        s3://downloads.robocorp.com/action-server/beta\"\n  aws s3 cp s3-drop/version.txt\
        \ $S3_BASE_URL/version.txt --cache-control max-age=120 --content-type \"text/plain\"\
        \n  aws s3 cp s3-drop/windows64/action-server.exe $S3_BASE_URL/windows64/action-server.exe\
        \ --cache-control max-age=120 --content-type \"application/octet-stream\"\n\
        \  aws s3 cp s3-drop/macos-arm64/action-server $S3_BASE_URL/macos-arm64/action-server\
        \ --cache-control max-age=120 --content-type \"application/octet-stream\"\n\
        \  aws s3 cp s3-drop/linux64/action-server $S3_BASE_URL/linux64/action-server\
        \ --cache-control max-age=120 --content-type \"application/octet-stream\"\n\
        fi\n"
    - name: Wait for Downloads S3 Bucket to have the right content
      timeout-minutes: 5
      run: "\nEXPECTED_VERSION=${RELEASE_REF#actions-runtime-}\nVERSION_URL=\"https://cdn.sema4.ai/action-server/releases/${EXPECTED_VERSION}/version.txt\"\
        \necho \"Expected version: $EXPECTED_VERSION\"\nwhile true; do\n  DOWNLOADED_VERSION=$(curl\
        \ -fsS --max-time 10 \"$VERSION_URL\")\n  echo \"Downloaded version: $DOWNLOADED_VERSION\"\
        \n  echo \"Expected version: $EXPECTED_VERSION\"\n    if [ \"$DOWNLOADED_VERSION\"\
        \ = \"$EXPECTED_VERSION\" ]; then\n      echo \"Versions match.\"\n      break\n\
        \    else\n      echo \"Versions do not match. Retrying in 30 seconds.\"\n\
        \    fi\n    sleep 30\n    done\n"
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
    - name: Trigger Brew Deploy Workflow
      run: 'curl --fail-with-body --max-time 30 -X POST            -H "Authorization:
        token ${{ secrets.GH_PAT_GHA_TO_ANOTHER_REPO }}"            -H "Accept: application/vnd.github.v3+json"            https://api.github.com/repos/sema4ai/homebrew-tools/actions/workflows/publish.yml/dispatches            -d
        "$(jq -cn --arg version "${RELEASE_REF#actions-runtime-}" ''{ref:"main",inputs:{version:$version}}'')"'
      env:
        RELEASE_REF: ${{ inputs.release_ref }}
run-name: 'Runtime recovery: ${{ inputs.release_ref }} @ ${{ inputs.release_sha }}'


# Important!!
# This file is auto-generated by running:
#
#   python _gen_workflows.py
#
# Do not edit it directly, your changes will be lost!
# Instead, edit `_gen_workflows.py` with the required
# changes and rerun that script (or manually change
# this file, but before the final version
# make sure that you update the `_gen_workflows.py`
# script to match the changes you made here
# and rerun the script).
